spring:
  main:
    web-application-type: none
    banner-mode: off
  ai:
    chat:
      model: ${PATCHIE_CHAT_MODEL:qwen2.5-coder:7b}
      base-url: ${PATCHIE_CHAT_BASE_URL:http://localhost:11434}

logging:
  pattern:
    console: "%d{HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n"
  level:
    io:
      steviemul:
        patch: INFO